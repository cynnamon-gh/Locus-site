# Resend — https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxx

# Verified sender in Resend (e.g. onboarding@resend.dev for testing, or your domain)
RESEND_FROM_EMAIL=Locus <onboarding@resend.dev>

# Inbox(es) that receive contact form submissions.
# One address, or several separated by commas. Read server-side only,
# so these are never exposed to visitors.
CONTACT_EMAIL=you@example.com, someone-else@example.com

# Public site URL for Open Graph (set to your Railway domain after deploy)
NEXT_PUBLIC_SITE_URL=https://your-app.up.railway.app
